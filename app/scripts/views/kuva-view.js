/*global define*/
define([
    'backbone',
    'hbs!tmpl/kuva'
], function(Backbone, Template) {

    'use strict';
    
       var HaasteView = Backbone.View.extend({

        template: Template,
        
       })
       
       return KuvaView;
    }
